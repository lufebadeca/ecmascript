def spread(p, lista):  #function groups the passed args
    print(p)
    for index, elem in enumerate(lista):
        print("elem %d: %s" %(index, elem ))

spread('alpha',(0,5,9,8,7,14,'luis')) #args are passded with no agrupation